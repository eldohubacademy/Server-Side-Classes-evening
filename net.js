const os = require("os"); // os module--- info about our operating system

console.log(os.freemem());

// memory -- RAM
console.log(os.networkInterfaces());

// access to computers network inteface can allow us to listen to incoming request to our ip address -- web server
